require("config.options") -- editor options (before plugins: netrw off etc.)
require("config.lazy") -- plugin manager + plugins
require("config.keymaps") -- plugin-independent keymaps
require("config.autostart") -- open tree + terminal on startup
require("config.smartclose") -- :q/:wq that never break the layout
require("config.autosave") -- VS Code-style auto save
require("config.winguard") -- keep files/terminals in their own windows
