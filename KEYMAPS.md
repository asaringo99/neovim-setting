# Neovim キーバインド一覧

リーダーキー = **スペース**。**スペースを押して少し待つと候補一覧がポップアップ**します(which-key)。`:Telescope keymaps` でインクリメンタル検索も可能。

## 基本

| キー | 動作 |
|---|---|
| `jk` (挿入モード) | Esc(ノーマルモードへ戻る) |
| `Ctrl+S` | 保存(挿入モード中でも可)。自動保存はデフォルトOFF(`:AutoSaveToggle`でフォーカス切替時保存をON) |

## 選択・コピー (VS Code風)

| キー | 動作 |
|---|---|
| `Shift+矢印` | 選択開始・拡張(そのまま文字を打つと置換) |
| `Ctrl+←` / `Ctrl+→` | 単語単位で移動(→は単語の末尾へ。挿入モードでも可) |
| `Ctrl+Shift+←→` | 単語単位で選択を拡張 |
| `Ctrl+F` | ファイル内検索(打ちながらジャンプ、Enter確定 → `n`/`N` で次/前へ、`Ctrl+L` でハイライト消去) |
| `Ctrl+H` (ノーマル) | 直前のCtrl+F検索ヒットを一括置換(置換文字を打ってEnter。Enter前に`c`で1件ずつ確認) |
| `Ctrl+H` (選択中) | 選択範囲内だけ置換 |
| 矢印(無修飾) | 選択解除 |
| `Ctrl+C` (選択中) | Windowsクリップボードへコピー(これだけがクリップボードに触る) |
| `"+p` | Windowsクリップボードから貼り付け |
| `y` / `d` / `p` | 従来通りvim内部レジスタのみ(クリップボードを汚さない) |

## ファイルツリー (nvim-tree)

| キー | 動作 |
|---|---|
| `Ctrl+B` / `Space e` | ツリーの表示/非表示 |
| `Space o` | ツリーにフォーカス |

ツリー内での操作:

| キー | 動作 |
|---|---|
| `Enter` | 開く |
| `a` / `r` / `d` | 新規作成 / リネーム / 削除 |
| `D` | ゴミ箱へ |
| `x` / `c` / `p` | 切り取り / コピー / 貼り付け |
| `gy` | 絶対パスをコピー |
| `Ctrl+]` | カーソル下のフォルダをルートに (CD) |
| `-` | ルートを1つ上へ |
| `g?` | ツリー内ヘルプ(全キー一覧) |

開いたファイルにツリーは自動追従(autoReveal 相当)。

## 検索 (telescope / grug-far)

| キー | 動作 |
|---|---|
| `Space ff` / `Ctrl+P` | ファイル名検索 |
| `Space fg` | 全文検索(プロジェクト全体) |
| `Space fh` | 全文検索+rg引数 例: `foo -g!*.js apps/backend` |
| `Space fw` | カーソル下の単語で全文検索 |
| `Space fb` | 開いているバッファ一覧 |
| `Space fr` | 最近開いたファイル |
| `Space fH` | Neovim ヘルプ検索 |
| `Space sr` | 検索/置換パネル (grug-far、一括置換可) |
| `Space sw` | カーソル下の単語で grug-far |
| `Space sf` | 現在ファイル内だけで grug-far |

telescope ピッカー内:

| キー | 動作 |
|---|---|
| `Ctrl+N` / `Ctrl+P` (↑↓も可) | 候補移動 |
| `Enter` | 開く |
| `Ctrl+V` / `Ctrl+X` | 縦分割 / 横分割で開く |
| `Ctrl+Q` | 全マッチを quickfix へ |
| `Esc` | 閉じる |

## ターミナル (toggleterm)

| キー | 動作 |
|---|---|
| `Ctrl+J` | 開く / フォーカス / 隠す(状況で自動判断、挿入モードで入る) |
| `2` `Ctrl+J` | 2番ターミナル(3以降も同様) |
| `Space ts` | ターミナル一覧から選択 |
| `Ctrl+K` (ターミナル内) | 上のエディタへ(ターミナルは表示のまま) |
| `Enter` (ターミナル・スクロールモード) | カーソル下のファイルパスを開く(下線が出たら開ける。path:12で行ジャンプ) |
| `Esc` (ターミナル内) | 常にスクロールモードへ(実行中コマンドのログ遡りOK) |
| `Ctrl+\` → `Esc` (ターミナル内) | 常に本物のEscをアプリへ送る(保険) |
| `i` (ターミナル内ノーマル) | 入力モードに戻る |

## ファイルタブ (bufferline)

| キー | 動作 |
|---|---|
| `Shift+L` / `Shift+H` | 次 / 前のタブ |
| `Space bp` | 文字を打ってタブへジャンプ |
| `Space bd` | 現在のバッファを閉じる |
| `Space bo` | 他のバッファを全部閉じる |
| マウスクリック | タブ切替(右クリックで閉じる) |

## LSP (コードジャンプ・リファクタ)

| キー | 動作 |
|---|---|
| `gd` / `gD` | 定義 / 宣言へジャンプ |
| `Ctrl+O` / `Ctrl+I` | ジャンプ元へ戻る / 進む(連打可・ファイル跨ぎOK) |
| `gi` | 実装へジャンプ |
| `gr` | 参照一覧 |
| (自動) | カーソル下の単語と同じ箇所が自動ハイライト。`Alt+N`/`Alt+P` で次/前の出現へ |
| `K` | ホバー(型・ドキュメント) |
| `Space rn` | シンボルのリネーム |
| `Space ca` | コードアクション(自動修正等) |
| `Space F` | フォーマット |
| `[d` / `]d` | 前 / 次の診断へ |

コマンド: `:checkhealth vim.lsp`(アタッチ確認)、`:lsp restart`、`:Mason`

## 補完 (blink.cmp) — 挿入モード

| キー | 動作 |
|---|---|
| `Enter` / `Tab` | 候補を確定 |
| `Ctrl+N` / `Ctrl+P` | 候補移動 |
| `Ctrl+E` | メニューを閉じる(その後 Enter は改行) |
| `Ctrl+Space` | 手動でメニュー/ドキュメント表示 |
| `Tab` / `Shift+Tab` (スニペット中) | 次 / 前のプレースホルダ |

## Git (gitsigns / telescope)

| キー | 動作 |
|---|---|
| `Space gs` | 変更ファイル一覧(git status)から選んでジャンプ |
| `]c` / `[c` | 次 / 前の変更箇所へ |
| `Space hp` | 変更内容をポップアップ diff |
| `Space hs` | その変更ブロックを stage |
| `Space hr` | その変更ブロックを破棄 |
| `Space hb` | その行の blame |
| `Space hd` | ファイル全体を分割 diff |

## ウィンドウ操作

| キー | 動作 |
|---|---|
| `Space w` | ウィンドウ選択(ツリー/エディタ/ターミナル一覧 → C-n/C-p で移動 → Enter) |
| `Space tl` | ツリー/ターミナルのレイアウト優先切替(ターミナル全幅 ⇔ ツリー全高) |
| `Alt+↑↓←→` | 現在ウィンドウのサイズ変更 |
| マウスドラッグ | 境界線を掴んでサイズ変更 |
| `Ctrl+W` → `h/j/k/l` | 左/下/上/右のウィンドウへ移動 |
| `Ctrl+W` → `=` | 全ウィンドウ均等化 |

## 標準キーを上書きしている箇所(注意)

| キー | 本来の動作 | 現在の動作 |
|---|---|---|
| `Ctrl+B` | 1ページ上スクロール | ツリー開閉(代替: `Ctrl+U`) |
| `Shift+H` / `Shift+L` | 画面の最上/最下行へ | タブ切替(代替: `gg` / `G`) |
| `gi` | 最後の挿入位置で insert | LSP 実装ジャンプ |
| `Ctrl+J` (ターミナル内) | 改行 | トグル(Enter を使用) |
| `Ctrl+K` (ターミナル内) | 行末まで削除 | エディタへ移動 |
